import React from 'react';
import Layout from '../../components/Layout';
import { Picture } from '../../components/Picture';

/**
* @author
* @function Home
**/

export const TakePicture = (props) => {
  return(
    <Layout>
      <Picture />
    </Layout>
   )

 }